{
  "name": "latitude-model-benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "bench": "node --env-file=.env src/run.ts",
    "smoke": "node --env-file=.env src/run.ts --models mock --tasks 01-interval-merge --trials 1",
    "validate-tasks": "node src/validate-tasks.ts",
    "report": "node src/report.ts"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "latest",
    "@ai-sdk/google": "latest",
    "@ai-sdk/openai": "latest",
    "@ai-sdk/otel": "latest",
    "@latitude-data/telemetry": "latest",
    "@opentelemetry/api": "^1.9.1",
    "ai": "latest",
    "zod": "^4.0.0"
  }
}
